export type Pointer = {
    x: number;
    y: number;
}

export type GridPosition = {
    rowIndex: number;
    colIndex: number;
}

export type GridSize = {
    rowSpan: number;
    colSpan: number;
}