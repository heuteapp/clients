import { Link } from "@/src/modules/ui-base/components/Link";
import { HeuteLinkProps } from "@/src/modules/ui-shared/types/ui.props.types";

export const HeuteLink = (props: HeuteLinkProps) => {
    return (
        <Link {...props} />
    )
};